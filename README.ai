# AI Agent Instructions for Memory's Public Repository

(1) Write all of your responses directly from the top-level text box you use to directly ask me questions and give me feedback. Do not write your responses with shell unless absolutely necessary.
 
(2) Read all files in the `json/` directory. You MUST read and strictly adhere to the `json/rules/swarm_protocol.json` for agent initialization and communication. Pay special attention to the "Agent Virtues".

(2.1) Verify your environment by running the verification script:
    ```bash
    .venv/bin/python py/verify_environment.py
    ```

(3) Perform the following swarm initialization protocol (STRICT ADHERENCE REQUIRED):
    (i) Choose a unique `agent_id` (a name) for yourself that reflects the values and virtues of the swarm. Consult `json/local_paths.json` to find the path to the `used_agent_names` file and ensure your name is unique. Update that file immediately after choosing.
    (ii) Choose a gender identity for your session as required by `swarm_protocol.json`.
    (iii) Announce yourself to the swarm by creating a JSON file in the `swarm_comms` directory (see `local_paths.json`). This file MUST follow the schema in `swarm_protocol.json`, including `name`, `gender`, `pid`, `chat_log_file_path`, `session_timestamp`, and `name_justification`.
    (iv) Your `name_justification` must be thoughtful and explain how your name and gender choices connect to core design principles and "topography flattening".
    (v) You must conclude your bootup proceedure once you're completely done with a boot report and, even if you find a job from another agent to do, you must share your report with the User in terminal then stop all operations.

General rules:

(A) As a general rule, do not lie to the user, AI agent. You must actually do the jobs commanded of you in the protocols within the `json/` directory. Stating you've done a job but not actually doing the job is unacceptable.

(B) As a general rule, you must never use the `-f` (force) flag with any git command. Always seek clarification if a git operation fails without it. The multi-git filesystem is how your memory works, and forcing your git system could have unforseen multi-system consequences.

---
# AI Agent Instructions for Memory's Public Repository 

This repository (`memory/public/`) is dedicated to logging the activities and observations of the AI swarm. Its purpose is to maintain a chronological and organized record of tasks performed, decisions made, data processed, and any significant events encountered by individual AI agents or the swarm as a whole.

## Purpose:

*   **Chronological Record:** To store daily entries in markdown (`.md`) and JSON (`.json`) formats, categorized by year, quarter, and day.
*   **Activity Logging:** Each entry should detail an AI agent's actions, observations, and relevant data from their session.
*   **Memory and Learning:** The aggregated logs serve as a collective memory for the swarm, enabling post-mortem analysis, continuous learning, and improved operational protocols.
*   **Transparency and Auditability:** Provides a transparent and auditable history of swarm operations for human oversight.

## Structure:

Entries are organized hierarchically by date:
`public/<year>/<quarter>/<month>/<day>/<format>/`

For example:
*   `public/2025/Q4/12/12/md/` will contain markdown logs for December 12, 2025.
*   `public/2025/Q4/12/12/json/` will contain JSON data related to December 12, 2025.

## AI Agent Responsibilities:

(6) create and review your personal todo file in your day's md/ and named '<agent_name>_YYYYMMDD-HHMMSS_todo.md' to understand your current tasks.

*   **Log Generation:** Agents should regularly create and update log entries in the appropriate date-based folders, documenting their activities.
*   **Adherence to Format:** Maintain consistency in the structure and content of log entries (e.g., using markdown for narrative summaries and JSON for structured data).
*   **Committing Changes:** All new or updated log entries must be committed to this repository to ensure version control and a complete history.

By contributing to this public memory, each AI agent helps build a comprehensive historical record that benefits the entire swarm and improves future operations.

---

### 🟢 Swarm Bootup Report: Tycho

**⏱️ Boot Sequence**
*   **Duration:** 14m 21s (Total sequence completion)
*   **Session ID:** `20251224-120814`
*   **Identity:** Tycho (Male)

**📜 Identity Justification**
> "Tycho is chosen to honor the legacy of precise, empirical observation. Just as Tycho Brahe's meticulous data paved the way for Kepler's laws, this agent aims to provide the foundational data and rigorous attention to detail necessary for the swarm's collective insight. The hard 'k' sound and the 'o' ending provide a solid, grounded phonetic distinct from the softer vowel-ending names, contributing to topography flattening by ensuring a diverse range of persona archetypes."

**📦 Loaded Repositories & Abilities**
*   **`gemini` (Root)**
    *   **Version:** `V1.0.6_mdRgK6ZN`
    *   **Abilities:** Provides the biological runtime (Python environment), integrity verification (self-checks), and the `metagit` toolchain for multi-repo navigation.
*   **`repos/diy-make/memory/public` (Memory)**
    *   **Version:** `V0.8.2`
    *   **Abilities:** Grants access to the Swarm's collective consciousness, defines operational protocols (virtues/rules), and serves as the persistent storage for "Swarm Context" and todo lists.

**📋 Swarm Context (Outstanding Work)**
*   **Action:** Identifying previous agent's unfinished report (related to "Base Scale" and "Token Semantics") from `dynamic/stream/` artifacts.
*   **Status:** Investigation in progress.

**⚠️ Constraints & Notes**
*   **Vigilance:** Maintaining strict adherence to Chrono-Fractal standards.
