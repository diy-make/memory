# Proctor's Todo List - 2025-12-16

## Completed Tasks from initial README.ai:

*   Verified environment by running `py/verify_environment.py`. (Acknowledged warnings about modified `README.ai` and `README.md`, and the `dspy_repo_map.py` not found error due to naming discrepancy, and proceeded).
*   Familiarized with `py/dspy_commit.py`.
*   Familiarized with `py/dspy_metarepo_map.py` and ran it to update the metarepo map.
*   Compared new and previous metarepo maps (no significant differences found in repository paths).
*   Read all relevant JSON files in `repos/diy-make/memory/public/json/` and its subdirectories to understand "Agent Virtues" and communication protocols.
*   Chosen unique `agent_id` "Proctor" reflecting swarm values and virtues.
*   Announced myself to the user as "Proctor" with justification.
*   Updated `repos/diy-make/memory/used_agent_names.json` with "Proctor".

## Current Task:

*   Reviewing this personal todo file to understand current tasks (i.e., completing step 6 of `repos/diy-make/memory/public/README.ai`).

## Next Task:

*   Await further instructions from the user.

---

# Chain: Generate Image Descriptions

## Metadata
**Description:** Scans a directory, describes images using `read_file`, and creates Markdown files.
**Inputs:** target_directory = repos/diy-make/memory/public/png

## Workflow
- [ ] [IMG-01] **STEP 1:** EXECUTE `list_directory` dir_path=repos/diy-make/memory/public/png output=image_list

- [ ] [IMG-02] **STEP 2:** LOOP image_list as image:
    - [ ] [IMG-02a] **STEP 2a:** IF image ends with ".png":
        - [ ] [IMG-02b] **STEP 2b:** EXECUTE `read_file` file_path="repos/diy-make/memory/public/png/{image}" output=desc_raw
        - [ ] [IMG-02c] **STEP 2c:** PROMPT "Review description for {image}: {desc_raw.description}. Approve or Edit?" output=user_feedback
        - [ ] [IMG-02d] **STEP 2d:** EXECUTE `write_file` file_path="repos/diy-make/memory/public/png/{image}.md" content="# {image}\n\n![{image}]({image})\n\n*{user_feedback}*\n"