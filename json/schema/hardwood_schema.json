{
  "version": "2.2",
  "attribution": "Thrasymachus (20260102-001919)",
  "title": "Hardwood Schema Specification",
  "description": "Standardized schema for Hardened Heartwood artifacts. Mandates the \"values: []\" pattern for collections.",
  "definitions": {
    "hardwood_item": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "rule": {
          "type": "string"
        },
        "rationale": {
          "type": "string"
        },
        "description": {
          "type": "string"
        }
      }
    },
    "hardwood_collection": {
      "type": "object",
      "properties": {
        "values": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/hardwood_item"
          }
        }
      }
    }
  },
  "wedo_integration": {
    "rule": "Heartwood creation is managed via WeDo task-runners. All new records must be validated against this schema before being grafted into the trunk.",
    "path": "json/wedo/"
  }
}