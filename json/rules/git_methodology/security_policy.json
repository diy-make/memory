{
  "name": "security_policy",
  "rule": "Ensure no private keys, secrets, or other sensitive credentials are ever committed to the repository.",
  "path_to_scripting": "This rule is implemented as a pre-commit hook within the `dspy_commit.py` script. Before any commit, the script will scan the staged changes for sensitive information and block the commit if any is found."
}
