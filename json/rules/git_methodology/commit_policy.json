{
  "name": "commit_policy",
  "target_branch": "stage",
  "auto_propose_commit": true,
  "auto_commit": true,
  "condition": "After making and verifying a change. This includes changes made during iterative refinement, unless explicitly instructed otherwise by the user.",
  "path_to_scripting": "This rule is implemented by the `dspy_commit.py` script. The agent's core logic will call this script after any successful file modification, and the script will handle the commit process as defined by the flags in this rule."
}
