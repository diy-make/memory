{
  "name": "Identify Task",
  "description": "Determine the stuck agent's last task by reviewing the swarm communication logs in `the directory specified by `swarm_comms` in `json/local_paths.json``. Look for messages indicating the agent's current objective."
}